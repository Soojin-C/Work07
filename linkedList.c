#include <stdio.h>
#include <stdlib.h>
#include "linkedList.h"

//struct node {int i; struct node * next}

void print_list (stuct node * pointer){

  while (pointer* != NULL){

    printf("%d |", pointer*);
    pointer++;
      
  }
       
}

struct node * insert_front (struct node * pointer, int val){

  
  
}

struct node * free_list (struct node * pointer){

  
  
}
